# Backuper

If the AndroidManifest file has the allowbackup flag set to true, you can run this script and make the backup.

This script requires that you stay connected in your device!

Usage: ./backuper com.example.app
